- [Schematic format layout](schematic_format_layout.md) — WorldEdit/MCEdit `.schem` structure: hybrid MCEdit-wrapped variant, `Offset` (paste anchor) vs `WorldEdit.Origin` (//copy -e player pos), varint-encoded `Data` byte array, why prismarine-schematic doesn't work in this project.
- [extract-blocks script](https://github.com/sandstone-mc/summit_2026_booth/blob/main/scripts/extract-blocks.ts) — `scripts/extract-blocks.ts` in sandstone-mc/summit_2026_booth: extract block positions from `.schem` to a TS `as const` array; `--append` with `@extract-blocks:` markers makes re-runs idempotent.